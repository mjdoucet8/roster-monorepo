# blueprints

Use this folder for proven patterns that can be reused.

A blueprint should capture an approach that worked: the problem it solves, when it applies, implementation shape, tradeoffs, and examples. It should be more durable than a one-off plan and more concrete than general advice.

Create a blueprint after a feature, workflow, architecture pattern, or operational practice has succeeded well enough that the team may want to repeat it.
